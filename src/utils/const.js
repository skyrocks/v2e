// 常量类
window.__C = {
  // 存储在cookie种的键名: 当前的api token
  K_TOKEN: 'token',
  // 存储在cookie种的键名: 从cas服务返回的token
  K_CASTOKEN: 'cas-token',

  // sidebar状态, 0, 1
  K_SIDEBARSTATUS: 'sb',

  // 分页查询每页默认记录数
  P_PAGESIZE: 50,

  // 页面引导手册标志键名, 存储的是json
  K_GUIDE: 'guide'
}
